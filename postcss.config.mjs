/** @type {import('postcss-load-config').Config} */
export default {
  plugins: {
    // Tailwind and Autoprefixer for CSS processing
    tailwindcss: {},
    autoprefixer: {},
  },
};

// PostCSS config updated
// Note: Autoprefixer ensures cross-browser compatibility for modern CSS features.
